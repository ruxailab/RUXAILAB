codecov:
  require_ci_to_pass: true

coverage:
  status:
    project:
      default:
        target: 60%
        threshold: 5%
        informational: true
    patch:
      default:
        target: 70%
        informational: true

comment:
  layout: "reach,diff,flags,files"
  behavior: default
  require_changes: false
